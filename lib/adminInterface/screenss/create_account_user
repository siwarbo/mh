import 'package:flutter/material.dart';

class CreateUserAccount extends StatelessWidget {
  const CreateUserAccount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create User Account'),
      ),
      body: Center(
        child: Text('User creation page'),
      ),
    );
  }
}
